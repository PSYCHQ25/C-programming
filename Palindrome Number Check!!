#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {
    int N, original, reversed = 0;
    scanf("%d", &N);

    original = N;

    
    if (N < 0) {
        printf("Not Palindrome\n");
        return 0;
    }

    while (N > 0) {
        int digit = N % 10;
        reversed = reversed * 10 + digit;
        N /= 10;
    }

    if (original == reversed) {
        printf("Palindrome\n");
    } else {
        printf("Not Palindrome\n");
    }

    return 0;
}
    
 

