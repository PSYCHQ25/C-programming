#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    // Handle negative numbers
    if (N < 0) {
        N = -N;
    }

    int lastDigit = N % 10;

    // Find the first digit
    int firstDigit = N;
    while (firstDigit >= 10) {
        firstDigit /= 10;
    }

    printf("%d %d\n", firstDigit, lastDigit);

    return 0;
}
