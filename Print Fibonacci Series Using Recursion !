#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int fibonacci(int n) {
    if (n == 0) return 0;
    if (n == 1) return 1;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

void printSeries(int i, int n) {
    if (i == n)   
        return;

    printf("%d ", fibonacci(i));
    printSeries(i + 1, n);   
}

int main() {
    int n;
    scanf("%d", &n);

    printSeries(0, n);

    return 0;
}
