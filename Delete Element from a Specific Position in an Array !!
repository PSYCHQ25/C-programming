#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N); 

    int arr[N];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]); 
    }

    int position;
    scanf("%d", &position);

    if(position < 1 || position > N) {
        printf("Invalid position\n");
        return 0;
    }

    
    for(int i = position - 1; i < N - 1; i++) {
        arr[i] = arr[i + 1];
    }
    N--; 

    
    for(int i = 0; i < N; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}

