#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N); 

    int arr[N];
    for(int i = 0; i < N; i++) {
        scanf("%d", &arr[i]); 
    }

    int output[N]; 
    int uniqueCount = 0;

    for(int i = 0; i < N; i++) {
        int isDuplicate = 0;
        
        for(int j = 0; j < uniqueCount; j++) {
            if(arr[i] == output[j]) {
                isDuplicate = 1;
                break;
            }
        }
        if(!isDuplicate) {
            output[uniqueCount] = arr[i];
            uniqueCount++;
        }
    }

    if(uniqueCount == 0) {
        printf("No elements left\n");
    } else {
        for(int i = 0; i < uniqueCount; i++) {
            printf("%d ", output[i]);
        }
        printf("\n");
    }

    return 0;
}
