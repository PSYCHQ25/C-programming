#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {
    int N, i, isPrime = 1;
    scanf("%d", &N);

    if (N <= 1) {
        isPrime = 0;  
    } else {
        int limit = (int)sqrt(N);
        for (i = 2; i <= limit; i++) {
            if (N % i == 0) {
                isPrime = 0;
                break;
            }
        }
    }

    if (isPrime)
        printf("Prime\n");
    else
        printf("Not Prime\n");

    return 0;
}

   
