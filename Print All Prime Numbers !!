#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>


bool isPrime(int num) {
    if (num < 2)
        return false;  
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0)
            return false;  
    }
    return true;  
}

int main() {
    int start, end;
    int found = 0; 

    
    scanf("%d %d", &start, &end);

    
    for (int i = start; i <= end; i++) {
        if (isPrime(i)) {
            printf("%d ", i);
            found = 1;
        }
    }

    
    if (!found) {
        printf("No prime numbers found");
    }

    return 0;
}
